{
  "name": "@snappin/api-types",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "generate:api": "dotenv -e ../../apps/web/.env -- sh -lc 'swagger-typescript-api generate -p \"$SWAGGER_API_URL\" -r -o ./src --modular -d --extract-request-body --extract-response-body --extract-response-error --axios'"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "swagger-typescript-api": "^13.2.16"
  },
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  }
}
